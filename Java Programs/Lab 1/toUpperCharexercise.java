public class toUpperCharexercise
{
    public static void main(String args[])
    {
        char theChar;
        char upperChar;

        theChar = 'f';
        upperChar = Character.toUpperCase(theChar);

        System.out.println("The uppercase of theChar is " + upperChar);
    }
}
